interface:
  display_name: "QA Executor"
  short_description: "Run QA cases, sync evidence, and auto-prepare Notion reports from discovered links."
  default_prompt: "Use $qa-executor to execute approved QA cases, auto-discover Notion test sources from Plane/text when present, sync managed Plane and Notion output after confirmation, and produce execution_result.json."
