interface:
  display_name: "QA Executor"
  short_description: "Run QA cases and capture evidence."
  default_prompt: "Use $qa-executor to execute approved QA cases and produce execution_result.json."
