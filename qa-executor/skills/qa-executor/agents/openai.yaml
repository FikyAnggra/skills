interface:
  display_name: "QA Executor"
  short_description: "Run QA cases and update source rows with evidence."
  default_prompt: "Use $qa-executor to execute approved QA cases, update the original source row/item after each case, embed or upload image evidence when supported, keep Plane output summary-only, create report documents only when explicitly requested, and produce execution_result.json."
