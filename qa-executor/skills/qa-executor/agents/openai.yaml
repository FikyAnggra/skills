interface:
  display_name: "QA Executor"
  short_description: "Run QA cases and sync evidence."
  default_prompt: "Use $qa-executor to execute approved QA cases, optionally sync managed Plane and Notion output with bridge links, and produce execution_result.json."
