interface:
  display_name: "QA Executor"
  short_description: "Run QA cases, capture evidence, sync tools."
  default_prompt: "Use $qa-executor to execute approved QA cases, optionally sync managed Plane or Notion output, and produce execution_result.json."
